# springboot-playground

Before running the application, make sure to get the containers up through docker compose. Two postgres (maybe it could be one?) and a keycloak instance will be started.

```
docker compose up
```
